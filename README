How to compile an executable:
Use "python3 build.py" to create executable ./main_engine

Running the executable:
./main_engine

Note: the program can easily and safely be exited by pressing the close button at the top of the window

Prerequisites assumed:
SDL installed
Machine supports OpenGL 3.3 

Attribution:
SDLGraphicsProgram baseplate - Mike Shah
glad/glad.h and glad.c - GLAD
build.py - Mike Shah